import React from 'react';

const SearchBar = () => {
    return (
        <div>
            <input placeholder={"search"}/>
            <button>search</button>
        </div>
    );
};

export default SearchBar;