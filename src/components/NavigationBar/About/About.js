import React from 'react';
import CreateRepo from "../../createRepo/createRepo";
import {Button, ChakraProvider, Spinner, Stack, useToast} from "@chakra-ui/react";

const About = () => {
    // const toast = useToast()

    return (
        <div>
            <h1>About us page</h1>

        </div>
    );
};

export default About;