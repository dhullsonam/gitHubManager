import React from 'react';
import SearchBar from "../../SearchBar/SearchBar";

const HomePage = () => {
    return (
        <div>
          <h1>home page</h1>
        </div>
    );
};

export default HomePage;